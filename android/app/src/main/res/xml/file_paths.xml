<?xml version="1.0" encoding="utf-8"?>
<!--
  Exposes only the traces directory. A recording that cannot leave the phone is
  not much use, but nothing else in the app's storage needs to be shareable.
-->
<paths>
    <external-files-path name="traces" path="traces/" />
</paths>
